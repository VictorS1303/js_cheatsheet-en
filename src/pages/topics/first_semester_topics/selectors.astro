---
import Layout from '../../../layouts/Layout.astro'
import BackButton from '../../../components/BackButton.astro'
import DescriptionCard from '../../../components/DescriptionCard.astro'
import CodePreviewCard from '../../../components/CodePreviewCard.astro'
import DescriptionCardHeader from '../../../components/DescriptionCardHeader.astro'
import DescriptionCardText from '../../../components/DescriptionCardText.astro'
import DescriptionCardExampleLink from '../../../components/DescriptionCardExampleLink.astro'
---

<Layout title="JS Cheat Sheet | Selectors">
    <BackButton className="back-button" id="back_button" href="../../first_semester" buttonText="Til oversigt"/>

    <div class="container desc-container selectors-desc-container">
        <DescriptionCard class="card what-card">
            <div class="card what-card">
                <DescriptionCardHeader class="what-header" headerText="Hvad er selectors?" />

                <DescriptionCardText>
                    En selector bruges til at vælge et bestemt element i ens HTML.
                    Der er et hav af selectors, men der er tre primære, som man skal kende.
                    Disse er <span class="class">klasse</span>, <span class="id">id</span> og <span class="element">element</span>, og du kan finde uddybende eksempler til, hvilke JavaScript-metoder man bruger til at vælge elementer baseret på enten <span class="class">klasse</span>, <span class="id">id</span> eller selve <span class="element">elementet</span>, samt hvad forskellen på de tre er, <DescriptionCardExampleLink href="https://codepen.io/Schweitz/pen/BagNqza" linkText="her." target="_blank" />
                </DescriptionCardText>
            </div>
        </DescriptionCard>

        <DescriptionCard class="card syntax-card">
            <div class="card syntax-card">
                <DescriptionCardHeader class="syntax-header" headerText="Syntaks" />
                <DescriptionCardText>
                    Syntaksen for at tilgå elementer i CSS ved brug af enten <span class="class">klasse</span>, <span class="id">id</span> eller <span class="element">elementet</span> selv er forholdsvist simpelt:

                    <br />
                    
                    En <span class="class">klasse</span> tilgås i CSS'en med et <span class="class">punktum</span> efterfulgt af <span class="class">klassens</span> navn.

                    <br />
                    Et <span class="id">id</span> tilgås i CSS'en med et <span class="id">#</span> efterfulgt af <span class="id">id</span>'ets navn.

                    <br />
                    Et <span class="element">element</span> tilgås i CSS'en ved at skrive <span class="element">elementets</span> navn.
                </DescriptionCardText>
            </div>

            <div class="code-preview-card">
                <CodePreviewCard>
                    <h3>
                        <pre>
                            <span class="class">Klasse: .klasse-navn</span>
                            <span class="id">Id: #id_navn</span>
                            <span class="element">Element: elementnavn</span>
                        </pre>
                    </h3>
                </CodePreviewCard>
            </div>
        </DescriptionCard>
    </div>
</Layout>