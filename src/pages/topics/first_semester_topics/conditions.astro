---
import Layout from '../../../layouts/Layout.astro'
import BackButton from '../../../components/BackButton.astro'
import DescriptionCard from '../../../components/DescriptionCard.astro'
import CodePreviewCard from '../../../components/CodePreviewCard.astro'
import DescriptionCardHeader from '../../../components/DescriptionCardHeader.astro'
import DescriptionCardText from '../../../components/DescriptionCardText.astro'
import DescriptionCardExampleLink from '../../../components/DescriptionCardExampleLink.astro'
import TopicPageNavbar from '../../../components/TopicPageNavbar.astro'
---


<Layout title="JS Cheat Sheet | Conditions">
    <TopicPageNavbar>
        <h1 class="logo">
            <span>JS</span> Cheat Sheet
        </h1>
        <BackButton className="back-button" id="back_button" href="/first_semester/" buttonText="Til oversigt"/>
</TopicPageNavbar>
    
    <div class="container desc-container">
        <DescriptionCard class="card what-card">
            <div class="card what-card">
                <DescriptionCardHeader class="what-header" headerText="Conditions" />

                <DescriptionCardText>
                    Conditions are used to check, if an expression is true of false.
                    Imagine that you only have space for ten items in your basket.
                    Each time you add an item to your basket, you check, if it's full.
                    This is first true, when you add the tenth item, and there's no more space for any more items.
                </DescriptionCardText>
            </div>
        </DescriptionCard>

        <DescriptionCard class="card what-card">
            <DescriptionCardHeader class="syntax-header" headerText="Syntaks" />

            <DescriptionCardText>
                An <span class="keyword">if</span> sentence starts with writing the word <span class="keyword">if</span> followed by a pair of <span class="parentheses">parentheses</span> containing <span class="condition">the condition</span>. After that comes a pair of <span class="curly-brackets">curly brackets</span>, inside which the <span class="code">code</span> to be run is.
                There's more to this, which can be found in the example <DescriptionCardExampleLink href="https://codepen.io/Schweitz/pen/NWZxNNZ" linkText="her." target="_blank" />
            </DescriptionCardText>

            <div class="code-preview-container">
                <CodePreviewCard>
                    <h3>
                        <pre>
                            <span class="keyword">if</span> <span class="parentheses">(<span class="condition">condition</span>)</span>  <span class="curly-brackets">&#123;</span>
                            &nbsp;&nbsp;&nbsp;&nbsp;<span class="code preview-card-code">// Code here</span>
                            <span class="curly-brackets">&#125;</span>
                        </pre>
                    </h3>
                </CodePreviewCard>
            </div>
        </DescriptionCard>
    </div>
</Layout>